version: '3'

services:
  postgres:
    image: postgres:16.0
    container_name: nestjs
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 1
      POSTGRES_DB: nestjs
    ports:
      - "5433:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
